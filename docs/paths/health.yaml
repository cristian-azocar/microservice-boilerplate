get:
  summary: Returns the current status of the service
  tags:
    - health
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: '../schemas/responses/health.yaml'
